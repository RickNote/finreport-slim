"""
主题关键词配置示例
==================
将此文件复制为 themes.py，填入实际关键词后放置于 finreport_scope 项目目录。

每个主题对应一组关键词，用于在年报页面中打分筛选相关页。
关键词出现次数总和达到 --min-score 阈值的页被标记为"命中"。

如何添加新主题
--------------
在 THEME_PRESETS 字典中新增一个条目，格式如下：

    "theme-name": {
        "description": "主题说明",
        "keywords": ["关键词1", "关键词2", ...],
    }
"""

from __future__ import annotations
from typing import Any

THEME_PRESETS: dict[str, dict[str, Any]] = {

    # ------------------------------------------------------------------
    # 示例主题 1：资产负债表
    # ------------------------------------------------------------------
    "balance-sheet": {
        "description": "合并资产负债表主表及附注映射",
        "statement_patterns": [
            r"合并资产负债表",
        ],
        "note_start_patterns": [
            r"财务报表附注",
        ],
        "note_stop_patterns": [
            r"受托业务",
        ],
        "keywords": [
            # 填入资产负债表相关关键词
            "关键词A",
            "关键词B",
        ],
    },

    # ------------------------------------------------------------------
    # 示例主题 2：自定义主题
    # ------------------------------------------------------------------
    "custom-theme": {
        "description": "自定义主题说明",
        "keywords": [
            # 填入与该主题高度相关的词语，建议 4-8 个
            "关键词1",
            "关键词2",
            "关键词3",
        ],
    },

}
